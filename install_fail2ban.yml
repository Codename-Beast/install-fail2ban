---
- name: Install Fail2Ban protection
  hosts: "{{ hosts | default(install_fail2ban_hosts | default('fail2ban_targets')) }}"
  become: true
  gather_facts: true
  roles:
    - role: install_fail2ban
