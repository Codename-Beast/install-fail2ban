[Definition]
# Managed by Ansible role install-fail2ban.
#
# Behavioral scanburst filter. It counts HTTP error responses for real request
# lines only; the high maxretry in the jail decides when the pattern becomes a ban.
#
# Regex breakdown:
#   ^(?:\S+:\d+\s+)?        optional "ip:port" prefix (e.g. reverse proxy log)
#   <HOST>                  client IP (Fail2ban placeholder)
#   \s+\S+\s+\S+\s+         logname + user fields ("- -" in Combined Log Format)
#   \[[^\]]*\]\s+           timestamp block, e.g. [26/Jul/2026:18:16:08 +0200]
#   "(?:[A-Z]+)             opening quote + HTTP method (GET, POST, ...)
#   \s+[^\"]+\s+            request path/target (anything until next space run)
#   HTTP/(0.9|1.0|1.1|2|2.0|3)" explicit HTTP version + closing quote
#   \s+(?:400|403|404|405|408|414)   client-error status codes only (no 401/5xx)
#   \s+(?:\d+|-)\s+"..."\s+"..."  strict Combined Log suffix: size, referer, UA, end of line

# Fail2Ban DateDetector strips the recognized timestamp text before failregex is applied;
# only the surrounding brackets remain as [], hence [^\]]* (zero-or-more)
# instead of [^\]]+ (one-or-more) in the timestamp field.
#
# Caveats:
# - This filter is intentionally broad and behavioral; the jail threshold, not
#   a single match, decides whether a source is banned.
# - The pattern anchors directly at the request field; there is no ".*" before
#   the opening request quote, so forged request strings in referer/User-Agent
#   fields do not affect counting.

failregex = ^(?:\S+:\d+\s+)?<HOST>\s+\S+\s+\S+\s+\[[^\]]*\]\s+"(?:GET|POST|HEAD|OPTIONS|PUT|DELETE|PATCH|PROPFIND|CONNECT|TRACE)\s+[^\s"]+\s+HTTP/(?:0\.9|1\.0|1\.1|2(?:\.0)?|3)"\s+(?:400|403|404|405|408|414)\s+(?:\d+|-)\s+"[^"]*"\s+"[^"]*"$
ignoreregex =
