[Definition]
# Managed by Ansible role install-fail2ban.
#
# Behavioral scanburst filter. It counts HTTP error responses for real request
# lines only; the high maxretry in the jail decides when the pattern becomes a ban.
failregex = ^(?:\S+:\d+\s+)?<HOST>.*"(?:[A-Z]+)\s+[^\"]+\s+HTTP/\d(?:\.\d)?"\s+(?:400|403|404|405|408|414)(?:\s+.*)?$
ignoreregex =
